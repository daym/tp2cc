#pragma once
// p2cc driver. No public API -- `main.cc` is the entry point and
// never included from another translation unit. This header exists
// only to satisfy the Makefile's `src/%.cc src/%.h` pattern rule.
